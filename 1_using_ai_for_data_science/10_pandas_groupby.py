# Finish coding this pandas operation below so that I get the
# mean, median, and standard deviation of TOR_WIDTH for each TOR_F_SCALE

import pandas as pd

# Load the dataset
df = pd.read_csv("./data/tornado.csv")

# Group by TOR_F_SCALE and calculate mean, median, standard deviation for TOR_WIDTH
grouped_stats = None # Placeholder

print(grouped_stats)