# Use FastAPI to make a service to get all orders and return then in JSON format
# Let's be very careful to not expose sensitive data in production
# To expose our database to a service.
# This is just a first pass at using FastAPI
# and it may go well, or not so well!
